node_modules/
out/
# What "remember ... as ..." keeps, and what programs write next to themselves
*.memory.json
build/
dist/
*.log
.DS_Store
Thumbs.db


# What a running program remembers, and the lock it holds while it runs.
*.busy
*.memory.json
